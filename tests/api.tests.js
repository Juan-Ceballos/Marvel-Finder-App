import { describe, test, it, expect } from "vitest";

describe('response', () => {
    it('should return data from marvel api data.length is greater than 2000', () => {
        // arrange
        const expectedDataLength = 2000

        // act
        const data = response()
        // assert
    })
})